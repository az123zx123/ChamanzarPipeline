[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "ChamanzarPipeline"
authors = [{name = "Xiang Li", email = "xli7@andrew.cmu.edu"}]
dynamic = ["version", "description"]
